{
  "name": "oamtm-submission-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API für OAMTM User-Submissions",
  "main": "worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "wrangler dev --test"
  },
  "dependencies": {
    "octokit": "^3.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
