{
  "name": "oamtm-serverfarm-static",
  "version": "1.0.0",
  "description": "Static site for OnAirMulTiMedia Serverfarm - Cloudflare Pages",
  "private": true,
  "scripts": {
    "build": "echo 'Static site - no build step needed'",
    "dev": "echo 'Static site - no dev server needed'",
    "preview": "echo 'Static site ready'",
    "cap:sync:android": "npx cap sync android",
    "cap:sync:ios": "npx cap sync ios",
    "cap:run:android": "npx cap run android",
    "cap:run:ios": "npx cap run ios",
    "cap:build:android": "npx cap build android",
    "cap:build:ios": "npx cap build ios",
    "build:electron": "npx electron-builder",
    "build:electron:win": "npx electron-builder --win",
    "build:electron:mac": "npx electron-builder --mac",
    "build:electron:linux": "npx electron-builder --linux"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ViewunitySystem/OnAirMulTiMedia.git"
  },
  "keywords": [
    "static-site",
    "cloudflare-pages",
    "serverfarm",
    "onairmultimedia",
    "capacitor",
    "electron",
    "pwa"
  ],
  "author": "Raymond Demitrio Dr. Tel",
  "license": "MIT",
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/app": "^5.0.0",
    "@capacitor/haptics": "^5.0.0",
    "@capacitor/keyboard": "^5.0.0",
    "@capacitor/status-bar": "^5.0.0"
  }
}
